{% extends 'base.html.twig' %}
{% block title %}{{ ad.title }}{% endblock %}
{% block body %}
    {{ ad.title }}
    {{ ad.game }}
    {{ ad.cond }}
    {{ ad.status }}
    {{ ad.content }}
    {{ ad.price }}
{% endblock %}

