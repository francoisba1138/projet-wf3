{% extends 'base.html.twig' %}

{% block body %}
    {{ dump(response) }}

    {% for game in response %}
        {{ game.name }}<br>
        {{ game.slug }}<br>
        {{ game.cover }}<br>
    {% endfor %}
{% endblock %}